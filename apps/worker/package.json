{
  "name": "@braincrew/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/worker.js",
  "scripts": {
    "dev": "tsx watch src/worker.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/worker.js",
    "lint": "eslint src test",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@braincrew/contracts": "workspace:*",
    "@braincrew/database": "workspace:*",
    "@braincrew/observability": "workspace:*",
    "@braincrew/orchestrator-dsh": "workspace:*",
    "bullmq": "^5.58.5",
    "dotenv": "^17.2.2",
    "ioredis": "^5.7.0",
    "pino": "^9.9.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "tsx": "^4.20.5"
  }
}
